# LLM Models

Available models:
https://platform.openai.com/docs/models/gpt-5-mini

I noted  a big disparity between GPT-4o-mini and 5 mini. In either case the model can be selected in the `config.js` file.